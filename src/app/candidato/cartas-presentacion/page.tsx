'use client'; import { FormEvent, useState } from 'react'; export default function CoverLetters(){const [saved,setSaved]=useState(false);return <div className="form-shell"><div className="eyebrow">Documentos de candidatura</div><h1>Cartas de presentación</h1><p className="muted">Crea cartas reutilizables y asócialas a un proceso cuando estés listo.</p><form className="form-card" onSubmit={(e:FormEvent)=>{e.preventDefault();setSaved(true)}}><label className="field"><span>Nombre de la carta</span><input required placeholder="Ej.: Carta para Oficial de Créditos" /></label><label className="field"><span>Contenido</span><textarea required rows={10} placeholder="Escribe tu carta con tus propias palabras." /></label>{saved&&<div className="notice">Carta guardada como borrador.</div>}<button className="button primary">Guardar carta</button></form></div>}
