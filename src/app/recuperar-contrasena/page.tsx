'use client'; import { FormEvent, useState } from 'react'; export default function Recover(){const [done,setDone]=useState(false);return <div className="form-shell"><div className="eyebrow">Acceso seguro</div><h1>Recuperar contraseña</h1><p className="muted">Te enviaremos instrucciones al correo asociado a tu cuenta.</p>{done?<div className="card"><h2>Solicitud recibida</h2><p>Si existe una cuenta con ese correo, recibirás instrucciones para restablecer tu contraseña.</p></div>:<form className="form-card" onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true)}}><label className="field"><span>Correo electrónico</span><input required type="email" /></label><button className="button primary">Enviar instrucciones</button></form>}</div>}
