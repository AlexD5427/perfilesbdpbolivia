'use client'; import { FormEvent, useState } from 'react'; import Link from 'next/link';
export default function CreateAccount(){const [done,setDone]=useState(false);function submit(e:FormEvent){e.preventDefault();setDone(true)}return <div className="form-shell"><div className="eyebrow">Candidatura BDP</div><h1>Crea tu cuenta</h1><p className="muted">Guarda tu perfil, prepara tu CV y postúlate con tranquilidad.</p>{done?<div className="card"><h2>Cuenta creada</h2><p>En el modo demostrativo, tu cuenta quedó lista para continuar. Revisa tu correo para verificarla cuando conectemos el proveedor de autenticación.</p><Link className="button primary" href="/candidato">Ir a mi espacio</Link></div>:<form className="form-card" onSubmit={submit}><div className="form-grid"><label className="field"><span>Nombre</span><input required name="firstName" /></label><label className="field"><span>Apellido</span><input required name="lastName" /></label><label className="field full"><span>Correo electrónico</span><input required type="email" name="email" /></label><label className="field"><span>Contraseña</span><input required minLength={8} type="password" name="password" /></label><label className="field"><span>Repite tu contraseña</span><input required minLength={8} type="password" name="passwordConfirm" /></label></div><label className="check"><input required type="checkbox" /> <span>Acepto los <Link href="/terminos">términos de uso</Link> y reconozco el <Link href="/privacidad">aviso de privacidad</Link>.</span></label><label className="check"><input type="checkbox" /> <span>Deseo recibir comunicaciones generales sobre oportunidades.</span></label><button className="button primary" type="submit">Crear cuenta</button><p className="muted">¿Ya tienes una cuenta? <Link href="/iniciar-sesion">Iniciar sesión</Link></p></form>}</div>}
