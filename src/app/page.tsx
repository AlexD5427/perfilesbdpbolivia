import Link from 'next/link';
import { jobs } from '@/lib/jobs';

export default function HomePage() { return <>
  <section className="hero"><div><div className="eyebrow">Talento que transforma</div><h1>Tu próxima oportunidad puede <span>mover Bolivia.</span></h1><p>Encuentra un lugar donde tu experiencia, tus ideas y tu compromiso contribuyan al desarrollo productivo de nuestro país.</p><div className="actions"><Link className="button primary" href="/procesos">Explorar oportunidades</Link><Link className="button secondary" href="/crear-cuenta">Crear cuenta</Link></div><p className="muted">Portal oficial de candidaturas · La Paz, Bolivia</p></div><div className="orbital" aria-label="Ilustración abstracta de conexión y crecimiento"><div className="orbital-card"><span className="tag">BDP · Personas y propósito</span><strong>Crecer contigo</strong><span>Un proceso claro, accesible y respetuoso.</span></div></div></section>
  <section className="section"><div className="section-head"><div><div className="eyebrow">Oportunidades abiertas</div><h2>Encuentra tu próximo reto</h2></div><Link href="/procesos">Ver todos →</Link></div><div className="grid">{jobs.slice(0,3).map((job) => <article className="card job-card" key={job.id}><div className="job-meta"><span className="tag">{job.area}</span><span>{job.city}</span></div><h3>{job.title}</h3><p className="muted">{job.summary}</p><Link className="button secondary" href={`/procesos/${job.id}`}>Ver cargo</Link></article>)}</div></section>
  <section className="section"><div className="feature-strip"><div className="card"><div className="eyebrow">01</div><h3>Postúlate con claridad</h3><p className="muted">Revisa la información del cargo y completa tu postulación paso a paso.</p></div><div className="card"><div className="eyebrow">02</div><h3>Tu información, bajo tu control</h3><p className="muted">Gestiona tu perfil, CV y cartas desde un solo lugar.</p></div><div className="card"><div className="eyebrow">03</div><h3>Accesibilidad desde el inicio</h3><p className="muted">Ajusta lectura, contraste y movimiento según tus necesidades.</p></div></div></section>
</>; }
