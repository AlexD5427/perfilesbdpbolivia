'use client'; import { FormEvent, useState } from 'react'; export default function ResetPassword(){const [done,setDone]=useState(false);return <div className="form-shell"><div className="eyebrow">Acceso seguro</div><h1>Restablecer contraseña</h1><p className="muted">Elige una contraseña nueva para proteger tu cuenta.</p>{done?<div className="card"><h2>Contraseña actualizada</h2><p>En el modo demostrativo, ya puedes iniciar sesión con tu nueva contraseña.</p></div>:<form className="form-card" onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true)}}><label className="field"><span>Nueva contraseña</span><input required minLength={8} type="password" /></label><label className="field"><span>Repite tu contraseña</span><input required minLength={8} type="password" /></label><button className="button primary">Restablecer contraseña</button></form>}</div>}
