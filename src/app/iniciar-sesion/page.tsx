'use client'; import { FormEvent, useState } from 'react'; import Link from 'next/link';
export default function Login(){const [sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <div className="form-shell"><div className="eyebrow">Tu espacio de candidatura</div><h1>Iniciar sesión</h1><p className="muted">Entra para continuar con tus postulaciones y documentos.</p><form className="form-card" onSubmit={submit}><label className="field"><span>Correo electrónico</span><input required type="email" /></label><label className="field"><span>Contraseña</span><input required type="password" /></label>{sent&&<div className="notice">Acceso de demostración habilitado. Te llevaremos a tu espacio.</div>}<button className="button primary" type="submit">Iniciar sesión</button><p><Link href="/recuperar-contrasena">¿Olvidaste tu contraseña?</Link></p><p className="muted">¿Primera vez aquí? <Link href="/crear-cuenta">Crear cuenta</Link></p></form></div>}
